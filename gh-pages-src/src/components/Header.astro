---
/**
 * Project: iamrichardd.github.io
 * Component: Header
 * File: Header.astro
 * Author: Richard D. (https://github.com/iamrichardd)
 * Purpose: Global navigation and brand identity component.
 * Traceability: Related to GitHub Issue #6
 */
import DarkModeToggle from './DarkModeToggle.astro';

const navLinks = [
  { name: 'Projects', href: '/projects' },
  { name: 'Pharos', href: '/projects/pharos' },
  { name: 'Resume', href: '/resume' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/80 dark:bg-secondary-900/80 backdrop-blur-md border-b border-secondary-200 dark:border-secondary-800">
  <div class="container-custom h-16 flex items-center justify-between">
    <!-- Brand -->
    <a id="header-brand" href="/" class="flex items-center gap-3 hover:opacity-80 transition-opacity">
      <img 
        src="https://avatars.githubusercontent.com/u/182445865?v=4&s=32" 
        alt="iamrichardD Logo"
        class="w-8 h-8 rounded-full border border-primary-200 dark:border-primary-700"
      />
      <span class="text-xl font-bold tracking-tight text-secondary-900 dark:text-secondary-100">
        iamrichardD
      </span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-8">
      {navLinks.map((link) => (
        <a 
          href={link.href}
          class="text-sm font-medium transition-colors text-secondary-600 dark:text-secondary-400 hover:text-primary-600 dark:hover:text-primary-400"
        >
          {link.name}
        </a>
      ))}
      <div class="h-6 w-px bg-secondary-200 dark:bg-secondary-800 mx-2"></div>
      <DarkModeToggle />
    </nav>

    <!-- Mobile Menu Button -->
    <div class="flex items-center gap-4 md:hidden">
      <DarkModeToggle />
      <button 
        id="mobile-menu-toggle"
        class="p-2 text-secondary-600 dark:text-secondary-400 hover:bg-secondary-100 dark:hover:bg-secondary-800 rounded-lg transition-colors"
        aria-label="Toggle mobile menu"
      >
        <svg id="menu-icon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
        <svg id="close-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Navigation Drawer -->
  <nav 
    id="mobile-menu"
    class="hidden md:hidden absolute top-full left-0 right-0 bg-white dark:bg-secondary-900 border-b border-secondary-200 dark:border-secondary-800 py-6 px-4 shadow-xl"
  >
    <div class="flex flex-col gap-4">
      {navLinks.map((link) => (
        <a 
          href={link.href}
          class="text-lg font-medium p-3 rounded-xl transition-colors text-center text-secondary-700 dark:text-secondary-300 hover:bg-secondary-100 dark:hover:bg-secondary-800"
        >
          {link.name}
        </a>
      ))}
    </div>
  </nav>
</header>

<script>
  function initMobileMenu() {
    const toggle = document.getElementById('mobile-menu-toggle');
    const menu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    const closeIcon = document.getElementById('close-icon');

    if (!toggle || !menu || !menuIcon || !closeIcon) return;

    let isOpen = false;

    toggle.addEventListener('click', () => {
      isOpen = !isOpen;
      
      if (isOpen) {
        menu.classList.remove('hidden');
        menuIcon.classList.add('hidden');
        closeIcon.classList.remove('hidden');
        document.body.style.overflow = 'hidden';
      } else {
        menu.classList.add('hidden');
        menuIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
        document.body.style.overflow = '';
      }
    });

    // Close menu on navigation
    menu.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        isOpen = false;
        menu.classList.add('hidden');
        menuIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
        document.body.style.overflow = '';
      });
    });
  }

  function initHeaderBranding() {
    const hasHeroBrand = document.body.dataset.hasHeroBrand === 'true';
    const headerBrand = document.getElementById('header-brand');
    const heroBrand = document.getElementById('hero-brand');

    if (!headerBrand) return;

    // If page doesn't have a hero brand, branding should always be visible
    if (!hasHeroBrand || !heroBrand) {
      headerBrand.classList.add('is-visible');
      return;
    }

    // Set up IntersectionObserver for pages with hero brand
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        // If hero brand is NOT intersecting the area below the 64px header, it means it's hidden under or above it
        // If it's above (entry.boundingClientRect.top < 64), then show the header brand
        if (!entry.isIntersecting && entry.boundingClientRect.top < 64) {
          headerBrand.classList.add('is-visible');
        } else {
          headerBrand.classList.remove('is-visible');
        }
      });
    }, {
      threshold: 0,
      rootMargin: '-64px 0px 0px 0px' // Height of the fixed header
    });

    observer.observe(heroBrand);
  }

  // Init on load and page changes
  initMobileMenu();
  initHeaderBranding();
  document.addEventListener('astro:page-load', () => {
    initMobileMenu();
    initHeaderBranding();
  });
</script>

<style>
  header {
    /* Offset main content so it's not hidden under the fixed header */
    transition: transform 0.3s ease-in-out;
  }

  #header-brand {
    transition: opacity 0.4s ease-in-out, transform 0.4s ease-out;
  }

  /* Initial state on pages with hero brand */
  :global(body[data-has-hero-brand="true"]) #header-brand {
    opacity: 0;
    transform: translateY(-8px);
    pointer-events: none;
  }

  /* Visible state (overrides hidden state on hero pages, and is default for others) */
  #header-brand.is-visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }
</style>
